import React from 'react';

import { Link } from 'react-router-dom';
const Main = () => {
    return (
        <div>
            <Link to={"/login"}>To Login page</Link>

        </div>
    );
};

export default Main;